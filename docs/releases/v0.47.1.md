## What changed

Added a comprehensive backlog document (`docs/backlog.md`) capturing all planned features, roughly prioritized:

1. **Global settings page** — central configuration UI, foundation for other features
2. **Folder rename / move resilience** — protect enrichment data when folders are reorganized
3. **Multiple collections / root folder switching** — surface the existing multi-library infrastructure in the UI
4. **Database import / export** — disaster recovery and machine migration
5. **Meaning search improvements** — model choice, re-indexing support
6. **Growing collections** — handling new folders as the library expands
7. **Camera upload auto-ingest pipeline** — watch folder, scan, tag, sort into the collection

Each entry includes the problem statement, possible approaches, existing infrastructure, affected code, and impact assessment.

## Verified

- Test suite: 79 tests, all passing (no code changes, only documentation).

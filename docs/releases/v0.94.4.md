The README badge row used `flat-square` for every badge, making the primary
badges (release, tests, license, sponsor) hard to spot at a glance.

Switched to a two-tier layout matching the Wireless Tools and
WiFiInstallationCheckup repos: `for-the-badge` for the top row of clickable
primary badges, `flat-square` for the secondary info row (Python version,
platform). The license badge is now also a link to the LICENSE file.

## Verified

- Full test suite: 101 tests passed.

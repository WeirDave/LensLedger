Direct feedback, fixed the same morning: a real full-width scan page with
real progress indicators, and the photo map actually working.

## Library health is now a real page

The old modal was too narrow, forced scrolling on a laptop, and gave
background jobs — model installs, OCR, face detection — zero visible
indication of progress. A disabled button and unchanging text looks
identical to "frozen" when a real install can take several minutes.

"Scan your photos" replaces it: a proper full-width page (up to 1366px),
and every long-running job now shows a genuine animated spinner plus live
elapsed time, not just a static line of text. A new "Photo locations
(GPS)" card also gives the location scan its own visible control — it was
previously an invisible side effect of "Rescan library."

## The photo map actually works now

Location scanning had simply never been run against the real library —
not a regression, a step that was always still pending. Ran it for real:
**845 photos located across 58 places**, and the map now shows them.

## Verified

- Full automated suite (56 tests, including new coverage for the scan
  page and its job cards).
- Manually verified live: the new page's width scales correctly, the
  spinner animation and elapsed-time text render and update against a
  real running scan, and the map genuinely shows real location data
  end-to-end.

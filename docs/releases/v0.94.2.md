The README badge row now includes a GitHub Sponsors badge, linking to
the repository's sponsor page. It uses the same `flat-square` style as the
existing release, tests, Python, platform, and license badges.

## Verified

- Full test suite: 101 tests passed.

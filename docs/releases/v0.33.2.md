Housekeeping release covering two small changes:

Removed a three-line block comment above `progressSuffix()` in
`web/js/scan-photos.js` that narrated what the function does ("Appends
'· 42% · ~3m remaining' to an elapsed-time string…"). The function name,
parameter list, and body already make this obvious — per the project's
"don't add comments explaining what code does" convention.

Also ships the AI assistant convention files added during v0.33.1:

- `CLAUDE.md` — instructions for Claude Code (product identity,
  versioning, commit format, release ceremony, don'ts)
- `CONVENTIONS.md` — the same rules in a format Aider reads
  automatically via `.aider.conf.yml`

These ensure every AI assistant working in this repo follows the same
release ceremony, commit subject format (`LensLedger vX.Y.Z — summary`),
and coding conventions.

## Verified

- Full automated suite: 79 tests, all green
  (`PYTHONPATH=src python -m unittest discover -s tests`).
- Comment-only change in JS; no functional impact.

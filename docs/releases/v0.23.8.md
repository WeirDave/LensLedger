Documentation only — nothing about how LensLedger runs changed.

## Every file and folder, explained

README.md now has a "Project layout" section: a plain-language map of every
root file and folder, and a table covering what each module under `src/`
actually does. The meaning-search instructions also now point at the new
in-app one-click "Set up meaning search" button first, with the manual
command kept as an alternative.

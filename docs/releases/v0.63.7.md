## Rename "Ingested" to "Imported"

Changed the auto-import stats key from `ingested` to `imported` across the pipeline, status page HTML, and JavaScript. Console output already said "imported" — this makes the stats and UI match.

## Verified

- Test suite: 80 tests passed.

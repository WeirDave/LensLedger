## What changed

Added a `.github/FUNDING.yml` file to enable the GitHub Sponsors button on the LensLedger repository. This links the sponsor button to the `WeirDave` GitHub Sponsors profile.

## Verified

- Test suite: 101 tests passed
- Confirmed `FUNDING.yml` follows the correct GitHub format

Updated to user-designed logo and favicon images.

## What changed

### New logo

The header logo (`assets/lensledger-logo.png`) now includes
"LensLedger" text in a vintage typewriter font above the camera
shutter/index card icon.

### Dedicated favicon

A new favicon image (`assets/lensledger-favicon.png`) shows the
camera shutter with "LL" initials — distinct from the header logo
and optimized for small sizes like browser tabs.

Added a `/favicon.png` route in `photo_search.py` that serves
this file. All six page templates updated their `<link rel="icon">`
from `/logo.png` to `/favicon.png`.

## Verified

- Test suite: 79 tests, all passing.

name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  tests:
    uses: ./.github/workflows/tests.yml

  release:
    needs: tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Verify release version and notes
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="${TAG#v}"
          python - "$VERSION" <<'PY'
          import re
          import sys
          from pathlib import Path

          expected = sys.argv[1]
          source = Path("product.py").read_text(encoding="utf-8")
          match = re.search(r'^APP_VERSION\s*=\s*["\']([^"\']+)["\']', source, re.MULTILINE)
          if not match or match.group(1) != expected:
              raise SystemExit(f"Release tag v{expected} does not match product.py")
          notes = Path("docs/releases") / f"v{expected}.md"
          if not notes.is_file():
              raise SystemExit(f"Missing release notes: {notes}")
          PY

      - name: Build release ZIP
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          ZIPNAME="LensLedger-${TAG}.zip"

          zip -r "$ZIPNAME" \
            app_paths.py \
            database_tools.py \
            face_learning.py \
            face_locations.py \
            generate_face_suggestions.py \
            generate_historical_folder_tags.py \
            lensledger_updater.py \
            library_config.py \
            metadata_reader.py \
            photo_index.py \
            photo_search.py \
            semantic_index.py \
            product.py \
            windows_ocr.ps1 \
            "Install LensLedger.cmd" \
            "Start LensLedger.cmd" \
            requirements.txt \
            requirements-semantic.txt \
            requirements-face.txt \
            README.md \
            LICENSE \
            THIRD_PARTY_NOTICES.md \
            CHANGELOG.md \
            assets/ \
            static/ \
            tools/ \
            -x "**/__pycache__/*" "**/*.pyc" "**/*.pyo"

          echo "ZIPNAME=$ZIPNAME" >> "$GITHUB_ENV"
          echo "TAG=$TAG" >> "$GITHUB_ENV"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: LensLedger ${{ env.TAG }}
          body_path: docs/releases/${{ env.TAG }}.md
          fail_on_unmatched_files: false
          files: ${{ env.ZIPNAME }}
